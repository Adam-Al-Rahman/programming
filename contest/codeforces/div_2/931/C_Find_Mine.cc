/*
 * @copyright
 * Copyright 2024 Adam-Al-Rahman
 *
 * @question:
 * @brief:
 * @source:
 * @tags: ["topics/", "algorithms/", "methods/"]
 * @status: solved
 */

/**
 * # mine: 2 (distinct cells)
 * # queries: 4
 * # mine locate: 1
 */

#include <cstdint>
#include <iostream>

void find_mine() {}

int main() {
  std::uint16_t tests;
  std::cin >> tests;

  while (tests--) {
    std::uint64_t n;
    std::cin >> n;

    std::uint64_t m;
    std::cin >> m;

    for (int i = 0; i < 4; i++) {
      std::uint16_t d;
      std::cin >> d;
    }
  }
}
